# Журнал задач (TASKS.md)

> Здесь ведётся учёт активных и завершённых задач проекта.
> `AGENTS.md` содержит только ссылку на этот файл — читайте его при старте новой сессии.

---

## Активные задач

Активных задач нет.

---

## Завершённые задачи
### ✅ Служебный «оборот, тыс.руб.» на листе «исходники ОПУ» для RUB
- **Дата:** сессия 02.09.2026
- **Суть:** в итоговом Excel у рублёвых компаний на листе «исходники ОПУ» столбец «оборот, тыс.руб.» всегда равен «оборот, тыс.ед.» (перевод в рубли не выполняется) и вызывает недоумение у пользователя.
- **Решение:** перед сохранением `save_results` вызывает `_prepare_journal_for_output` (pipeline/executors.py): при `needs_conversion=False` столбец удаляется из копии journal_df для файла; `context.journal_df` не изменяется. Константа `RUB_AMOUNT_COL` перенесена в `OpuReportConstants` (pipeline/step_config.py), шаг 19 ссылается на неё вместо литерала.
- **Инварианты:** для валютных компаний столбец сохраняется (в нём результат конвертации); листы «Расшифровка_ББЛ»/«Расшифровка_ОПУ»/«исходники ББЛ» не затронуты; сверки и метрики шагов работают в валюте компании, как прежде.
- **Файлы:** `pipeline/executors.py` (_prepare_journal_for_output + вызов в save_results), `pipeline/step_config.py` (OpuReportConstants.RUB_AMOUNT_COL), `pipeline/steps/step_19_build_opu.py` (ссылка на константу)
- **Итог:** реализовано, покрыто смоуком (_smoke_rub_fx_control.py, секция 8): RUB — столбец удалён, AED — сохранён, None проходит насквозь.

---

### ✅ Ошибка контроля курса на шаге 17 для RUB-компаний
- **Дата:** сессия 02.09.2026
- **Суть:** при тестировании обработки отсутствия листа курса RUB-компания ошибочно падала на шаге 17: `MissingCurrencyRateError «В справочнике отсутствует курс для валюты 'RUB'… добавьте лист Курс_RUB»`. Контрольная точка `_log_rate_anomalies` вызывала `get_rate_median` → `_get_rates_df`, а в `_CURRENCY_RATE_KEYS` ключа для RUB нет (и не требуется — перевод в рубли для RUB не выполняется, Сумма_руб = Сумма). Локальный `except (ValueError, KeyError)` исключение не ловил: `MissingCurrencyRateError` наследуется от `ReferenceMismatchError` → `PipelineError`.
- **Решение:** ранний выход в `_log_rate_anomalies` при `needs_conversion(context) == False` — тот же паттерн защиты, что в `_prepare_fx_columns`. Одновременно покрыты обе контрольные точки (вход/выход шага 17).
- **Инварианты:** для валютных компаний обработка ошибки не изменена: при отсутствии листа курса `MissingCurrencyRateError` фатальна (в реальном прогоне возникает раньше — на шаге 14 в `add_ruble_amount_column`).
- **Файлы:** `pipeline/steps/step_17_add_other_income_and_expenses.py` (_log_rate_anomalies — защита needs_conversion); `_smoke_rub_fx_control.py` (одноразовый смоук, шаблон `_*` в .gitignore)
- **Итог:** реализовано. Смоук: RUB — обе контрольные точки и `_prepare_fx_columns` без ошибок; USD без листа курса — `MissingCurrencyRateError` сохраняется.

---

### ✅ Перевод валютных значений в рубли (AED/CNY)
- **Дата:** сессия 01.09.2026
- **Суть:** для иностранных компаний (учёт в дирхамах/юанях) конвертация сумм в рубли. Баланс — остатки × курс на дату расшифровки. ОПУ — каждая проводка × курс на дату операции.
- **Файлы:** `utils/currency_utils.py` (add_ruble_amount_column, get_rate_for_date_with_info), `cli/main.py` (дата баланса), `pipeline/steps/step_14_build_opu_foundation.py`, `pipeline/steps/step_19_build_opu.py`
- **Справочник:** листы `Курс_AED`/`Курс_CNY` в `Справочники.xlsx` → `REFERENCE_REGISTRY`
- **Итог:** реализовано полностью. Два столбца (`*_руб`) в итоговых отчётах.

---

### ✅ Аномалия среднего курса по расходам
- **Дата:** коммит `80d84c8`
- **Суть:** при малых долях распределения расходов курс улетал в бесконечность (`доля × оборот_руб_группы`).
- **Решение:** рублёвый эквивалент распределённых расходов считается через курс группы (`rate_группы = оборот_руб_группы / оборот_группы`), затем `оборот_распределенный_руб = оборот_распределенный × rate_группы`.
- **Файлы:** `pipeline/steps/base_expenses_step.py` (_distribute_expenses, _create_remainder_rows), `pipeline/steps/step_17_add_other_income_and_expenses.py` (_distribute_orphan_expenses, _adjust_revenue_for_vat — теперь в обоих столбцах)
- **Итог:** решено.

---

### ✅ Удаление околонулевых строк ОПУ в step_19
- **Дата:** сессия 01.09.2026
- **Суть:** в итоговом ОПУ были строки с `оборот, тыс.ед. ≈ 0` при `оборот, тыс.руб. ≠ 0` → аномальный курс.
- **Решение:** удаление строк с `abs('оборот, тыс.ед.') < 1e-6` ДО сверки ЧП vs НРП.
- **Файлы:** `pipeline/steps/step_19_build_opu.py` (ZERO_ROWS_EPSILON = 1e-6, _drop_zero_amount_rows)
- **Итог:** реализовано. Сверка ЧП vs НРП остаётся корректной.

---

### ✅ Расследование аномального курса 92,48 RUB/AED
- **Дата:** сессия 01.09.2026
- **Суть:** одна строка ОПУ с подразумеваемым курсом 92,48 (норма ~24–25). Проводка: Дт 91.02.1 / Кт 60.01, «Донской ГВРЦ ГБУ РО», БК000000304 от 01.02.2025.
- **Вердикт: НЕ ошибка.** Курсовая разница сторнирования в нетто-строке: начисление 01.02.2025 (курс 26,6333) + сторно 01.04.2025 (курс 23,2801). В АЕД начисление/сторно уничтожились (нетто 1 178,33), в рублях осталась курсовая разница 77 593 руб = 23 140 AED × Δкурс 3,3532.
- **Постоянные улучшения (оставлены):**
  - `tolerance_rate_deviation` (лист «Параметры», дефолт 0.3) — автоконтроль отклонения курса от медианы
  - Контрольные точки `_log_rate_anomalies` в step_17 (Фаза 3.3)
  - `_diagnose_rate.py` (одноразовый, не коммитится, шаблон `_*` в .gitignore)
- **Файлы:** `utils/currency_utils.py` (get_rate_median, get_rate_deviation_limit, автоконтроль в add_ruble_amount_column), `pipeline/steps/step_17_add_other_income_and_expenses.py` (_log_rate_anomalies), `config/defaults.py` (SCHEMA/DEFAULTS/TOLERANCE_DESCRIPTIONS для tolerance_rate_deviation)
- **Итог:** задача закрыта, правки расчёта не требуются.

---

### ✅ Строка «Курсовые разницы» в ОПУ
- **Дата:** сессия 01.09.2026
- **Суть:** хвосты сторнирования размазываются по нетто-строкам, выглядят как ошибка. Выделение в отдельную строку «Курсовые разницы».
- **Решение:** базовый курс группы = курс на дату первой (min) операции. FX-строка: ед=0, руб=хвост, доход_расход=«Курсовые разницы». Порог выделения — tolerance_rate_deviation.
- **Инварианты:** суммарные ед/руб по ОПУ не меняются → сверка ЧП vs НРП и ОПУ-руб vs баланс-руб в силе.
- **Файлы:**
  - `pipeline/step_config.py` (OpuReportConstants: FX_DIFFERENCE_LABEL, FX_COL, RUB_BASE_COL, FX_ABS_FLOOR)
  - `pipeline/steps/step_17_add_other_income_and_expenses.py` (GROUP_COLS → константа, _prepare_fx_columns, _split_fx_difference_rows, _merge_with_main_df + context)
  - `pipeline/steps/step_19_build_opu.py` (_drop_zero_amount_rows: FX-строки выживают через проверку доход_расход != FX_DIFFERENCE_LABEL)
- **Итог:** реализовано полностью. В кейсе: осн. 1,17833/31,382816389 + «Курсовые разницы» 0/77,593048.
